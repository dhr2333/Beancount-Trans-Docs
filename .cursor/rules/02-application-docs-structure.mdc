---
globs: docs/**/*.md,docs-dev/**/*.md
description: 应用层 - 文档结构规范（Diátaxis）
---

# 应用层：文档结构规范

本仓库用户文档遵循 [Diátaxis](https://diataxis.fr/) 框架：按读者需求把内容分成四类，**不要混用**（例如不要在「教程」里塞大段 API 表格，也不要在「参考」里写教学故事）。

## Diátaxis 四象限

| 类型 | 英文 | 读者问题 | 写作取向 | 典型内容 |
|------|------|----------|----------|----------|
| **教程** | Tutorial | 「跟着学，我能否顺利跑通？」 | 学习路径、固定步骤、可复现、由浅入深 | 快速入门、端到端演练 |
| **操作指南** | How-to guide | 「我要完成某件事，怎么做？」 | 面向任务、步骤清单、可跳读 | 导出账单、配置映射、排错流程 |
| **解释** | Explanation | 「为什么会这样？设计权衡是什么？」 | 背景、概念、取舍、与其他系统的关系 | 核心概念、架构思路、分类与标签 |
| **参考** | Reference | 「这个字段/命令的精确定义是什么？」 | 机械、完整、结构一致、便于检索 | 配置项表、CLI、错误码、默认值 |

### 写作约束（摘要）

- **教程**：必须有一条清晰「从头到尾」的路径；每一步可验证；避免罗列所有边界情况。
- **操作指南**：以**目标**命名（动词+对象）；先给最短路径，再链到解释或参考。
- **解释**：允许叙述与类比；避免变成逐步操作手册；需要时链接到操作指南或参考。
- **参考**：按字母或固定结构排列；**不写**教学性开场白；与实现保持同步。

### 与开发者文档（`docs-dev`）

`docs-dev` 中的内容同样适用 Diátaxis（多为操作指南 + 参考 + 少量解释）。与普通用户文档分离的是**受众与发布范围**，不是框架本身。

## Frontmatter 字段规范

所有文档文件必须在文件开头包含 YAML Frontmatter，参考现有文档：[docs/02-教程/01-快速入门.md](mdc:docs/02-教程/01-快速入门.md)、[docs/04-操作指南/账单导出方法.md](mdc:docs/04-操作指南/账单导出方法.md)（若存在）、[docs/05-归档/01-核心概念.md](mdc:docs/05-归档/01-核心概念.md)。

### Diátaxis 分类（推荐）

在 Frontmatter 中增加 **`diataxis`**，便于人类与自动化工具识别文档类型（取值须为下列之一）：

- `tutorial`：教程
- `how-to`：操作指南
- `explanation`：解释
- `reference`：参考

新建或大幅改版文档时应补齐；存量文档可在修改时逐步补上。

### 必需字段

```yaml
---
tags: []                    # 标签列表，通常为空数组
title: 文档标题              # 文档显示标题（中文）
slug: document-slug         # URL 路径标识符（英文，使用连字符）
type: Write                 # 文档类型，固定为 "Write"
date: 2025-08-21            # 创建日期（YYYY-MM-DD 格式）
created_time: 2025-08-21 13:10:02  # 创建时间（YYYY-MM-DD HH:mm:ss）
modify_time: 2025-11-14 16:14:00    # 修改时间（YYYY-MM-DD HH:mm:ss）
authors: dhr2333            # 作者标识符
doc_status: Published       # 文档状态：Published（已发布）或 Draft（草稿）
published:                  # 发布渠道列表
  - beancount-trans-docs
published_time: 2025-11-14T14:59:00  # 发布时间（ISO 8601 格式）
---
```

另可增加（推荐，见上文「Diátaxis 分类」）：

```yaml
diataxis: how-to            # tutorial | how-to | explanation | reference
```

### 字段说明

- **diataxis**：文档在 Diátaxis 中的类型；新建或改版时**应**填写，便于归类与侧栏排序
- **tags**：文档标签，通常为空数组 `[]`，如需添加标签使用数组格式 `['tag1', 'tag2']`
- **title**：文档标题，使用中文，清晰描述文档内容
- **slug**：URL 路径标识符，使用英文小写字母和连字符，如 `quick-start`、`core-concepts`
- **type**：固定为 `Write`，表示这是一篇文档
- **date**：文档创建日期，格式为 `YYYY-MM-DD`
- **created_time**：创建时间，格式为 `YYYY-MM-DD HH:mm:ss`
- **modify_time**：最后修改时间，格式为 `YYYY-MM-DD HH:mm:ss`
- **authors**：作者标识符，参考 `blog/authors.yml`
- **doc_status**：文档状态，`Published` 表示已发布，`Draft` 表示草稿
- **published**：发布渠道列表，通常包含 `beancount-trans-docs`
- **published_time**：发布时间，ISO 8601 格式，如 `2025-11-14T14:59:00`

### Frontmatter 示例

```yaml
---
tags: []
title: 核心概念
slug: core-concepts
type: Write
diataxis: explanation
date: 2025-09-08
created_time: 2025-09-08 11:14:14
modify_time: 2025-11-14 14:18:35
authors: dhr2333
doc_status: Published
published:
  - beancount-trans-docs
published_time: 2025-09-12 16:07:00
---
```

## 文件命名规范

### 命名规则

- **数字前缀**：使用两位数字前缀表示文档顺序，如 `01-`、`02-`、`03-`
- **描述性名称**：使用中文描述性名称，清晰表达文档内容
- **文件扩展名**：统一使用 `.md` 扩展名
- **命名格式**：`数字前缀-描述性名称.md`

```text
// ✅ 正确示例
01-快速入门.md
04-操作指南/
  01-账户.md
  02-映射.md
03-解释/
  01-核心概念.md

// ❌ 错误示例
快速入门.md              # 缺少数字前缀
01_快速入门.md          # 使用下划线而非连字符
01-快速入门.markdown    # 使用 .markdown 而非 .md
```

### 目录结构示例（Diátaxis 对齐）

顶层目录按 **Diátaxis 类型** 划分（名称可微调，但语义应一致）：

```text
docs/
├── 02-教程/                 # Tutorial：学习路径、端到端
│   ├── 01-快速入门.md
│   └── ...
├── 03-解释/                 # Explanation：概念、设计、权衡（可从「归档」逐步迁入）
│   └── ...
├── 04-操作指南/             # How-to：任务导向、步骤清单
│   └── ...
├── 05-参考/                 # Reference：字段表、默认值、错误码
│   └── ...
└── 06-归档/                 # 历史或待归类；新内容不应默认写入此处
```

若仓库里仍是 `05-归档` 且无 `05-参考`，可暂时保留编号；**一旦新增「参考」类目录**，建议将归档顺延为 `06-归档`，避免出现两个 `05-*` 并列。

自托管等**开发者向**内容放在 `docs-dev/`，其中子目录同样建议按类型命名（如 `02-操作指南/`、`05-参考/`）。

## 目录组织规范

### 目录划分原则

- **按 Diátaxis 类型划分**：每个一级目录对应教程 / 操作指南 / 解释 / 参考之一；目录内再按主题分子目录
- **数字前缀排序**：使用数字前缀控制目录和文件的显示顺序
- **层级深度**：建议不超过 3 层目录嵌套
- **避免混放**：不要把「解释」长文塞进「操作指南」目录，除非该文以操作任务为主（可拆成两篇并互链）

### 目录命名规范

- **数字前缀**：目录名使用数字前缀，如 `02-教程`、`04-操作指南`
- **中文命名**：使用清晰的中文名称描述目录内容
- **目录结构**：保持目录结构扁平，避免过深嵌套

```text
// ✅ 推荐结构（Diátaxis）
docs/
├── 02-教程/
│   └── 01-快速入门.md
├── 04-操作指南/
│   └── 01-账单导出方法.md
└── 03-解释/
    └── 01-核心概念.md

// ❌ 不推荐：过深嵌套
docs/
└── 04-操作指南/
    └── 01-账单/
        └── 01-导出/
            └── 01-方法.md
```

## 文档层级和导航结构

### 侧边栏配置

文档的导航结构由 `sidebars.ts` 自动生成，使用 `autogenerated` 模式：

```typescript
const sidebars: SidebarsConfig = {
  tutorialSidebar: [{type: 'autogenerated', dirName: '.'}],
};
```

参考：[sidebars.ts](mdc:sidebars.ts)

### 文档层级建议

- **第一级**：Diátaxis 类型（教程 / 操作指南 / 解释 / 参考）
- **第二级**：主题域（如「账单」「映射」「部署」）
- **第三级**：单篇文档或子主题

### 文档组织原则

1. **先分类型，再分主题**：读者先选「我要学 / 我要做 / 我要懂 / 我要查」之一
2. **交叉链接而非合并**：教程结尾链到操作指南；操作指南链到解释与参考；避免单篇「全能文」
3. **循序渐进**：教程内保持线性；操作指南内步骤可独立跳转
4. **易于查找**：参考类文档标题与表格列名稳定、可排序

## 文档内容结构

### 按 Diátaxis 类型的结构要点

| 类型 | 开篇 | 正文 | 结尾 |
|------|------|------|------|
| 教程 | 你将完成什么、前提环境 | 编号步骤，每步可验证 | 下一步学什么 / 常见卡点 |
| 操作指南 | 适用场景与前提 | 最短步骤；分支用「若…则…」 | 相关解释、参考链接 |
| 解释 | 要回答的「为什么」 | 概念、历史、权衡、边界 | 链到操作指南或参考 |
| 参考 | 无叙事开场 | 表格、列表、固定小节 | 版本或兼容性注记 |

### 标准文档结构（通用骨架）

参考：[docs/02-教程/01-快速入门.md](mdc:docs/02-教程/01-快速入门.md)

```markdown
---
# Frontmatter（含 diataxis）
---

# 文档标题（由 Docusaurus 自动生成，Markdown 中从 ## 开始）

## 一、（教程：目标与前提）/（操作指南：要完成的事）/（解释：问题陈述）/（参考：范围说明）

## 二、主体（步骤 / 论证 / 表格）

## 三、总结、相关链接或「参见」
```

### 章节组织建议

- **教程、操作指南、解释**：可使用「一、二、三」作为一级标题，子节用 `###`
- **参考**：优先用固定小标题（如「字段说明」「默认值」「示例」），少用叙事性章节名
- **小节**：使用 `###` 作为三级标题

```markdown
## 一、总览：它是如何工作的？   <!-- explanation 示例 -->

### 1. 账本 (Ledger)

#### 账本的作用

这是详细说明。
```

## 文档状态管理

### 文档状态字段

- **Published**：已发布的文档，会在网站上显示
- **Draft**：草稿文档，不会在网站上显示

```yaml
---
doc_status: Published  # 或 Draft
---
```

### 发布流程

1. 创建文档时设置 `doc_status: Draft`
2. 完成编写和审核后，修改为 `doc_status: Published`
3. 更新 `modify_time` 和 `published_time`

## 文档维护规范

### 更新文档时

- **更新 modify_time**：每次修改文档时更新 `modify_time` 字段
- **保持 created_time**：`created_time` 字段不应修改，保持原始创建时间
- **更新 published_time**：如果文档状态从 Draft 变为 Published，更新 `published_time`

### 文档版本控制

- 使用 Git 管理文档版本
- 提交信息遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范
- 重要更新应在文档中记录变更历史

## 文档链接规范

### 内部链接

**重要说明**：由于文档会在 Obsidian 中编写，然后推送到远程仓库由 Docusaurus 构建，**必须使用完整 URL 或 Docusaurus 路由路径**，不要使用相对路径 `.md` 文件链接。

#### 推荐方式：完整 URL（适用于 Obsidian 和 Docusaurus）

使用完整 URL 可以确保链接在 Obsidian 和 Docusaurus 中都能正常工作：

```markdown
// ✅ 推荐：完整 URL（内部文档链接）
详细说明请参阅 [账户管理文档](https://trans.dhr2333.cn/docs/account)（URL 以各文档 `slug` 为准）
更多信息请查看 [核心概念](https://trans.dhr2333.cn/docs/core-concepts)

// ✅ 推荐：完整 URL（外部链接）
访问 [Beancount-Trans 控制台](https://trans.dhr2333.cn/login)
```

#### 备选方式：Docusaurus 路由路径（仅适用于 Docusaurus）

如果只在 Docusaurus 中使用，可以使用基于 slug 的绝对路径：

```markdown
// ✅ 备选：Docusaurus 路由路径（基于 baseUrl 和 slug）
详细说明请参阅 [账户管理文档](/docs/account)
更多信息请查看 [核心概念](/docs/core-concepts)
```

**注意**：这种方式在 Obsidian 中可能无法正确跳转，因为 Obsidian 无法解析 Docusaurus 的路由。

#### 不推荐：相对路径 `.md` 文件链接

**不要使用**相对路径链接 `.md` 文件，因为：

1. Docusaurus 使用 `slug` 字段生成 URL，而不是文件名
2. 相对路径在构建时可能无法正确解析
3. 文件重命名或移动会导致链接失效

```markdown
// ❌ 不推荐：相对路径 .md 文件链接
更多信息请查看 [核心概念](./01-核心概念.md)  // 在 Docusaurus 中可能无法工作
```

### 链接最佳实践

- **统一使用完整 URL**：为了兼容 Obsidian 和 Docusaurus，推荐统一使用完整 URL
- **基于 slug 构建 URL**：URL 路径基于文档的 `slug` 字段，而不是文件名
- **描述性链接文本**：使用清晰的描述性文本，避免"点击这里"
- **链接到相关文档**：在文档末尾添加"相关文档"或"下一步"部分；**跨 Diátaxis 类型**时优先链到对应类型（例如教程 → 操作指南，而非直接跳到参考大表）
- **检查链接有效性**：确保所有链接指向有效的文档或页面

### URL 路径规则

文档的 URL 路径由以下规则确定：

1. **基础路径**：`baseUrl`（配置为 `/docs/`）
2. **文档路径**：基于文档的 `slug` 字段
3. **完整 URL**：`https://trans.dhr2333.cn` + `baseUrl` + `slug`

示例：

- 文档 `slug: quick-start` → URL: `https://trans.dhr2333.cn/docs/quick-start`
- 文档 `slug: account`（在 `04-操作指南/` 等目录下时，URL 仍由 `slug` 决定，不一定含目录名）→ 以各文件 `slug` 为准

参考现有文档的实际链接使用：[docs/02-教程/01-快速入门.md](mdc:docs/02-教程/01-快速入门.md) 与站内其他已发布文档。
